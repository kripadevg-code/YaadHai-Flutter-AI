# Infraon Infinity — Claude Agent Rules

> Rules source of truth: `.ai/rules/`
> Read `.ai/rules/00-index.md` first, then follow rule files in order.
> Project context: `.ai/context/project-overview.md`
> Workflows: `.ai/workflows/`

## Build & Run Commands
```bash
melos bootstrap
melos run analyze
melos run test
cd apps/client && fvm flutter run
cd apps/infinity_agent && fvm flutter run
```
